#ifndef ITEMTYPES_H
#define ITEMTYPES_H

namespace ItemTypes {

static const int UserType = 65536;

static const int DiagramItemType = UserType + 1;

static const int ArrowItemType = UserType + 2;

static const int ArrowHandleItemType = UserType + 3;

static const int SizeHandleItemType = UserType + 4;

static const int DiagramTextItemType = UserType + 5;

static const int GraphicsItemGroupType = UserType + 6;

static const int HandleItemAppearAreaType = UserType + 7;

static const int PositionLineType = UserType + 8;

static const int SceneBoundaryType = UserType + 9;

static const int HandleItemType = UserType + 10;

}

#endif // ITEMTYPES_H
